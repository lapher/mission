#docker compose
version: '3'
services:
  jupyter:
    build: .
    container_name: jupyter-notebook
    ports:
      - "8888:8888"
    volumes:
      - /home/iii/mission/mission10/jupyter:/home/jovyan/work/
    command: start-notebook.sh --NotebookApp.token=''
  mariaDB:
    image: mariadb
    restart: always
    container_name: cc104.rds.local
    environment:
      MYSQL_ROOT_PASSWORD: iii
    volumes:
      - /home/iii/mission/mission10/mariaDB:/var/lib/mysql
